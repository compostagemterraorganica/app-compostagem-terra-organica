services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: http://localhost:5700
    container_name: terraorganica-web
    ports:
      - "5900:5900"
    environment:
      PORT: 5900
      NODE_ENV: production
    restart: unless-stopped
