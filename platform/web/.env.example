# URL da API Postgres (api/). Em Docker com mapeamento 5700:3000 use http://localhost:5700
# Em dev, use /api (proxy do Vite para localhost:3000) para cookies de sessao funcionarem.
VITE_API_URL=/api
