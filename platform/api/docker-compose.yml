services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5700:5700"
    env_file:
      - .env
    environment:
      PORT: 5700
      NODE_ENV: development
    volumes:
      - ./src:/app/src
    command: node --watch src/server.js
    restart: unless-stopped
